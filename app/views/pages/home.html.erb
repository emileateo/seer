<div class="landing-text">
  <div class="title">
    <h1>Seer</h1>
    <h2>Take destiny into your hands.</h2>
  </div>

  <p>Gone are the days where Feng Shui meant groaning as your mother dragged you to Chinatown before the start of a new school year.</p>
  <p>Get in touch with the Earth on your own terms.<br>Make your own story.</p>
</div>

<div class="new-user-form">
  <form name="new_user_form" novalidate="" action="/preview" class="user-input-form">
    <div class="user-data info">
      <span> I was born on&nbsp;</span>
      <input required="" type="date" placeholder="yyyy-mm-dd" pattern="\d\d\d\d-\d\d-\d\d" min="1500-01-01" max="2500-12-31" name="date" value="">
    </div>
    <div class="user-data info">
      <span>&nbsp;at&nbsp;</span>
      <input type="time" placeholder="hh:mm AM" pattern="(\d?\d):(\d\d)( (AM|PM|am|pm))?" name="time" id="birthTime" value="">.&nbsp;
    </div>
    <div class="header-btns">
      <input type="submit" class="button mt-2" value="Get a reading">
      <!-- gives params 'date' and 'time' -->
    </div>
  </form>
</div>

<!-- <div class="my-5">
  <p>Want to know more about...</p>
  <div class="cards container">
     <=  render Category.all %>
  </div>
</div> -->

<div class="dev-text my-5">
  <p>for development purposes only...</p>
  <% if user_signed_in? %>
    <p>You are signed in as <%= current_user.email %>.
    <%= link_to "Log out", destroy_user_session_path, :method => :delete, class: "nav-link" %></p>
  <% else %>
    <p>You are not signed in yet. <%= link_to 'Log in', new_user_session_path %> | <%= link_to 'Sign up', new_user_registration_path %></p>
  <% end %>
</div>

